name: flutter_clean_stuff
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  bloc:
  cupertino_icons:
  equatable:
  flutter:
    sdk: flutter
  flutter_bloc:
  fpdart:
  gap:
  get_it:
  injectable:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  injectable_generator:

flutter:
  uses-material-design: true
